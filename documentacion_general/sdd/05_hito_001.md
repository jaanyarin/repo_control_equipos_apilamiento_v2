# HITO DE DESARROLLO 001

| Campo | Valor |
|---|---|
| Hito | HDT-001 |
| Fecha | 2026-05-27 |
| Estado | Validado / Funcional / Operativo |
| Responsable | Jose Anyarin |
| Próximo Hito | HDT-002 (Tipos de Equipo, Proveedores, Equipos) |
| Rama Base | main |

---

## 1. Objetivo del Hito

Establecer la línea base funcional del sistema con los módulos fundamentales de infraestructura, autenticación, usuarios, sedes, campañas operativas y aplicación móvil Android (login). Todo desarrollo posterior parte de esta base sin necesidad de modificar lo ya validado.

---

## 2. Módulos Validados

| Código | Módulo | Estado |
|---|---|---|
| MOD-01 | Autenticación (Microsoft Entra ID OIDC) | ✅ Validado |
| MOD-02 | Usuarios (CRUD + eliminación física) | ✅ Validado |
| MOD-03 | Sedes (CRUD + eliminación física) | ✅ Validado |
| MOD-04 | Campañas (CRUD + activar/cerrar + eliminación física) | ✅ Validado |
| MOD-16 | Mobile App (Login OIDC + pantalla de ingreso exitoso) | ✅ Validado |
| — | Roles (lectura, filtrado por rol) | ✅ Validado |
| — | Infraestructura Docker (PostgreSQL + Nginx + Backend) | ✅ Validado |
| — | Base de Datos (Flyway V1/V2/V3) | ✅ Validado |
| — | Autenticación JWT + timezone America/Lima | ✅ Validado |

---

## 3. Actualización de Plataforma (Node.js + Expo SDK)

Previo a la validación del login mobile, se realizó una actualización de plataforma para compatibilidad con Expo Go v54 instalado en el dispositivo de prueba:

| Componente | Versión Anterior | Versión Actual (Validada) |
|---|---|---|
| Node.js | v20.9.0 | v22.22.3 LTS |
| Expo SDK | 52 | **54.0.35** |
| React | 18.3.1 | **19.1.0** |
| React Native | 0.76.x | **0.81.5** |
| expo-web-browser | ~13.x | ~15.0.11 |
| expo-auth-session | ~5.x | ~7.0.11 |
| expo-secure-store | ~13.x | ~15.0.8 |
| expo-linking | ~6.x | ~8.0.12 |
| react-native-paper | ^5.12.0 | ^5.12.0 |
| react-native-safe-area-context | 4.12.0 | ~5.6.0 |
| react-native-screens | ~4.5.0 | ~4.16.0 |
| expo-asset | ~10.x | ~12.0.13 |
| expo-constants | ~16.x | ~18.0.13 |
| expo-font | ~12.x | ~14.0.12 |
| expo-status-bar | ~1.x | ~3.0.9 |
| Hermes | No (JSC) | **Sí** (jsEngine: hermes) |
| New Architecture | No | **Sí** (newArchEnabled: true) |
| Entry Point | App.js | **expo/AppEntry** |

**Motivo**: El dispositivo de prueba (realme C21Y) tenía Expo Go v54 instalado, incompatible con SDK 52. Se optó por **Opción B** (actualizar Node.js + proyecto a SDK 54) en lugar de instalar Expo Go v52, para evitar tener que actualizar nuevamente al momento de compilar el APK final.

**Procedimiento**:
1. Node.js v20.9.0 actualizado a v22.22.3 (vía ZIP manual en `%USERPROFILE%\nodejs22\node-v22.22.3-win-x64`)
2. Se eliminó `node_modules` y `package-lock.json`
3. `npm install` con Node.js v22 — 873 paquetes instalados sin errores
4. Expo CLI v54.0.24, SDK del proyecto v54.0.0
5. Metro Bundler compila correctamente

---

## 4. Funcionalidades Verificadas

### 4.1 Infraestructura
- Contenedores Docker: PostgreSQL 18, Backend Quarkus, Nginx
- PostgreSQL con timezone America/Lima (UTC-5)
- Migraciones Flyway aplicadas hasta V3
- Frontend web SPA servido por Nginx, reverse proxy `/api/` al backend
- Backend accesible en `http://localhost/api/v1/`

### 4.2 Autenticación (MOD-01)
- Login con cuenta Microsoft corporativa vía OIDC (flujo authorization_code)
- Validación de dominio corporativo
- Validación de usuario registrado en dim_usuarios
- Validación de cuenta activa en Microsoft Graph API
- Validación de estado_activo en dim_usuarios
- JWT con claims: sub, upn, groups, nombre, correo, rolId
- Cierre de sesión con hard redirect (`?logout=1`)
- Manejo de errores de autenticación (`?error=`)
- Prevención de auto-login en logout/error

### 4.3 Usuarios (MOD-02)
- Listado de usuarios con datos corporativos sincronizados
- Creación de usuario con autocompletado de correo Microsoft Graph
- Edición de rol y estado activo
- Eliminación física con diálogo de confirmación
- Filtrado de roles según el rol del usuario logueado
- Roles con colores: Super Admin (verde), Admin (azul), Usuario (amarillo)

### 4.4 Sedes (MOD-03)
- CRUD completo (Crear, Listar, Actualizar, Eliminar)
- Un solo campo de entrada: "Sede" (nombre)
- Código auto-generado desde el nombre (mayúsculas, espacios → guion bajo, sin caracteres especiales)
- Eliminación física con confirmación (no soft delete)
- Validación de nombre único

### 4.5 Campañas (MOD-04)
- CRUD completo (Crear, Listar, Actualizar, Eliminar)
- Un solo campo de entrada: "Campaña" (nombre)
- Campos adicionales: fecha_inicio, fecha_fin
- Código auto-generado desde el nombre
- Solo una campaña activa a la vez
- Botones Activar/Cerrar por fila
- Fila activa resaltada en verde
- Eliminación física con confirmación (no soft delete)
- Estado mostrado como Chip: "Activa" (verde) / "Cerrada" (gris)

### 4.6 Mobile App (MOD-16)
- Login OIDC mediante WebBrowser nativo (expo-web-browser)
- Pantalla de ingreso exitoso: "Ingresaste de forma correcta" + nombre del usuario + correo
- Persistencia de sesión con SecureStore (token JWT)
- Botón "Cerrar sesión" que elimina el token y regresa al login
- Recuperación automática de sesión al abrir la app
- URL del backend configurable via variable de entorno `EXPO_PUBLIC_API_URL`
- Deep link callback (`exp://.../--/callback`) para recibir el token post-autenticación
- Sin navegación a Dashboard — pantalla de bienvenida como placeholder temporal
- Framework: Expo SDK 54 + React Native Paper + React Navigation

---

## 5. Documentación Asociada

| Documento | Descripción |
|---|---|
| `01_epecificaciones.md` | Especificaciones funcionales y RF |
| `02_planes.md` | Arquitectura y planificación |
| `03_tareas.md` | Backlog técnico |
| `04_implementaciones.md` | Estrategia de implementación |
| `05_hito_001.md` | **Este documento** — hito actual |

---

## 6. Decisiones Técnicas Validadas

| Decisión | Detalle |
|---|---|
| Soft delete | Datos operacionales → soft delete (estado_activo). Excepciones: usuarios, sedes, campañas → DELETE físico |
| Timezone | PostgreSQL configurado con `TZ=America/Lima` + `-c timezone=America/Lima`. Backend usa `OffsetDateTime.now(ZoneId.of("America/Lima"))` |
| Auto-generación de código | `nombre.toUpperCase().replaceAll("\\s+", "_").replaceAll("[^A-Z0-9_]", "")` |
| Campaña activa única | Al activar una campaña se desactiva automáticamente la anterior |
| Auth flow | Backend realiza fetch manual a Microsoft Entra ID (HttpClient), genera JWT propio |
| Frontend web serving | Vite build → nginx serve static files with SPA routing |
| Frontend mobile | Expo SDK 54 con React Native Paper, SecureStore, WebBrowser auth |
| API URL mobile | Configurable via `EXPO_PUBLIC_API_URL` en `.env` o variable de entorno |
| Logout web | Hard redirect con `window.location.href` + `?logout=1` |
| Logout mobile | Eliminación de token de SecureStore + reset de estado |
| Error handling | Backend redirige a `/?error=<mensaje>`, frontend muestra Alert |

---

## 7. Pendientes para Próximo Hito (HDT-002)

| Prioridad | Módulo | Descripción |
|---|---|---|
| Alta | MOD-07 | Tipos de Equipo — CRUD completo |
| Alta | MOD-08 | Proveedores — CRUD completo |
| Alta | MOD-06 | Equipos — CRUD, asociación a OSR, campañas, proveedores |
| Alta | MOD-09 | Averías — registro, atención, cálculo de tiempo inactivo |
| Alta | MOD-05 | PSR/OSR — registro documental |
| Alta | MOD-16 | Mobile App — agregar pantallas operativas (equipos, averías, etc.) |
| Media | MOD-10 | Evidencias Fotográficas |
| Media | MOD-11 | Dashboard KPI |
| Media | MOD-12 | Reportes PDF |
| Media | MOD-13 | Auditoría |
| Media | MOD-15 | Configuración general |

---

## 8. Instrucciones para Retomar el Desarrollo

Al retomar el desarrollo desde este hito:

1. **No modificar** los archivos de los módulos validados (sedes, campañas, usuarios, roles, auth, mobile login) a menos que sea estrictamente necesario por integración.
2. **Iniciar por HDT-002**: Tipos de Equipo, Proveedores, Equipos.
3. **Seguir el patrón** establecido por Sedes/Campañas:
   - Backend: Entity → Repository → Service → Resource (JAX-RS) + DTO + Mapper
   - Frontend web: Página en `frontend/src/pages/`, ruta en `App.jsx`, menú en `Layout.jsx`
   - Frontend mobile: Pantalla en `mobile/src/`, ruta en `App.js`
   - DB: Migración Flyway (`V4__*.sql`)
4. **Documentar** cada nuevo módulo en el próximo hito (`05_hito_002.md`).
5. **Para desplegar cambios web**: `npm run build` en `frontend/` y `docker compose up -d --build`
6. **Para mobile**: `cd mobile && npx expo start` y escanear QR con Expo Go
7. **Para cambiar de WiFi**: editar `mobile/.env` con la nueva IP y reiniciar Expo
8. **Verificar** que los endpoints retornen 401 sin token y 200/201/204 con token válido.

---

## 9. Verificación del Hito

Para confirmar que el hito está operativo:

```bash
# Contenedores funcionando
docker ps

# Frontend web accesible
curl -sk -o /dev/null -w "%{http_code}" http://localhost/
# → 200

# API protegida
curl -sk -o /dev/null -w "%{http_code}" http://localhost/api/v1/sedes
# → 401

# Mobile: cd mobile && npx expo start
# Escanear QR con Expo Go en celular
# Probar: login Microsoft → ver "Ingresaste de forma correcta" + nombre
# Probar: cerrar sesión → volver a pantalla de login
```

---

## 10. Notas Adicionales

- Las campañas y sedes existentes en la base de datos antes de este hito no tienen migración de datos asociada; se crean desde cero.
- La eliminación física de sedes fallará si hay usuarios con `sitio_id` referenciando esa sede (FK constraint). Es el comportamiento esperado.
- La eliminación física de campañas no tiene restricciones actualmente (FK no implementadas aún en tablas futuras).
- La aplicación mobile usa Expo Go para desarrollo; para APK final se requerirá compilar con `eas build`.
- El deep link `exp://.../--/callback` es manejado automáticamente por Expo Linking.

## 11. APK Android — Compilación y Validación (2026-05-29)

Se completó exitosamente la compilación del APK Android mediante EAS Cloud, su instalación en dispositivo físico (realme C21Y, Android 14) y ejecución correcta de la aplicación.

### 11.1 Versiones Finales del APK

| Componente | Versión en APK |
|---|---|
| Expo SDK | 54.0.35 |
| React | 19.1.0 |
| React Native | 0.81.5 |
| JS Engine | Hermes |
| New Architecture | Habilitada |
| Entry Point | expo/AppEntry |
| API URL | Configurable en runtime (SecureStore) |
| Build | EAS Cloud (no local) |

### 11.2 Problemas Resueltos Durante la Compilación

| Problema | Causa | Solución |
|---|---|---|
| `SoLoaderDSONotFoundError: libhermestooling.so` | `jsEngine: jsc` incompatible con newArchEnabled | Cambiar a `"jsEngine": "hermes"` en app.json y `hermesEnabled=true` en gradle.properties |
| `AppRegistry.registerComponent wasn't called` | Entry point incorrecto | Cambiar `"main": "App.js"` a `"main": "expo/AppEntry"` en package.json |
| Could not move temporary workspace (Gradle) | Sophos antivirus bloquea rename atómico | Usar EAS Cloud en vez de build local |
| API URL hardcodeada en build | EXPO_PUBLIC_API_URL en tiempo de compilación | Refactorizar a SecureStore runtime + interceptor Axios |

### 11.3 Build Local vs EAS Cloud

Se determinó que el build local en Windows con Sophos Endpoint Agent no es viable por el bloqueo del antivirus a las operaciones de Gradle. El build oficial se realiza mediante EAS Cloud.

Comando para build APK:
```bash
cd mobile
npm run build:android:apk
```

### 11.4 Configuración que NO debe modificarse

| Archivo | Clave | Valor | Consecuencia si se cambia |
|---|---|---|---|
| `app.json` | `jsEngine` | `"hermes"` | Crash: SoLoaderDSONotFoundError |
| `package.json` | `main` | `"expo/AppEntry"` | Crash: AppRegistry.registerComponent |
| `gradle.properties` | `hermesEnabled` | `true` | Crash: libhermestooling.so no encontrado |
| `gradle.properties` | `newArchEnabled` | `true` | Incompatible con versiones del stack |
| `src/api.js` | URL | SecureStore runtime | Si se vuelve a EXPO_PUBLIC_*, requiere rebuild por cada cambio de IP |

## 12. Estabilizacion Pre-APK 2026-05-29

Se ejecuto una revision tecnica previa al desarrollo del APK Android:

- Perfil tecnico alineado a React 18 + Vite, consistente con el frontend real.
- `docker-compose.yml` y `application.properties` ajustados para no mantener secretos reales como valores por defecto versionables.
- `mobile/app.json` limitado a Android y sin configuracion iOS fuera del alcance.
- Login mobile reforzado con estados de carga/error, pantalla de ingreso correcto y decodificacion JWT base64url.
- Backend actualizado a dependencias Quarkus REST vigentes (`quarkus-rest`, `quarkus-rest-jackson`) y version explicita de `maven-compiler-plugin`.
- Validaciones ejecutadas: build web, build backend, export Android Expo, health backend, endpoint protegido sin token y servicios Docker.

Riesgos pendientes:

- La carpeta local no contiene `.git`; antes de continuar se recomienda versionar formalmente el proyecto.
- EAS Build queda configurado; la firma Android se debe materializar durante `eas build` con credenciales gestionadas por Expo o keystore propio.
- Los secretos reales deben rotarse si fueron expuestos previamente en archivos compartidos.

---

_Fin del Hito de Desarrollo 001_

# HITO DE DESARROLLO 003

| Campo | Valor |
|---|---|
| Hito | HDT-003 |
| Estado | Implementado / Pendiente de Auditoría |
| Fecha de cierre | 2026-07-08 |
| Responsable | Jose Anyarin |
| Rama Base | main |
| Base de Datos Oficial | PostgreSQL 18 |
| Dependencia | HDT-002 validado |

---

## 1. Objetivo del Hito

Implementar calidad, despliegue y auditoría: completar el módulo Rol pendiente, agregar infraestructura de auditoría (backend + BD), crear paquetes de arquitectura faltantes (audit, config, security), establecer tests automatizados en backend/frontend/mobile, configurar CI/CD con GitHub Actions, e integrar modo claro/oscuro en frontend web.

---

## 2. Cambios Realizados

### 2.1 Backend — Módulo Rol Completado

| Capa | Archivo | Estado |
|---|---|---|
| Service | `RolService.java` | ✅ Nuevo |
| DTO | `RolDTO.java` | ✅ Nuevo |
| Mapper | `RolMapper.java` | ✅ Nuevo |
| Controller | `RolResource.java` | ✅ Refactorizado (sin lógica de negocio) |

**Refactor:** `RolResource` ahora delega toda la lógica a `RolService`, eliminando la violación de Clean Architecture.

### 2.2 Backend — Paquete `audit/` (Nuevo)

| Archivo | Propósito |
|---|---|
| `AuditoriaEvento.java` | Entidad JPA para `auditoria_eventos` |
| `AuditoriaRepository.java` | PanacheRepository con consultas personalizadas |
| `AuditoriaService.java` | Servicio para registrar y consultar eventos |
| `AuditoriaResource.java` | REST controller (`GET /auditoria/recientes`, `/por-entidad`, `/por-tipo`) |

### 2.3 Backend — Paquete `config/` (Nuevo)

| Archivo | Propósito |
|---|---|
| `AppConfig.java` | Configuraciones de aplicación (timezone, api version, jwt expiration) |
| `CorsConfig.java` | Filtro CORS para respuestas REST |

### 2.4 Backend — Paquete `security/` (Nuevo)

| Archivo | Propósito |
|---|---|
| `JwtFilter.java` | Filtro de autenticación JWT (rutas públicas excluidas) |
| `SecurityUtil.java` | Utilidad para extraer usuario e IP del contexto de seguridad |

### 2.5 Backend — Exceptiones

| Archivo | Propósito |
|---|---|
| `EntidadNoEncontradaException.java` | Excepción para recursos no encontrados |
| `ManejadorGlobalExcepciones.java` | ✅ Actualizado: maneja ParseException (JWT), ForbiddenException, EntidadNoEncontradaException |

### 2.6 Backend — JwtService

| Cambio | Detalle |
|---|---|
| ✅ Agregado `validarToken()` | Usa `JWTParser` de SmallRye para validación de tokens |

### 2.7 Flyway — Migraciones Nuevas

| Migración | Contenido |
|---|---|
| `V10__auditoria_eventos.sql` | Tabla `auditoria_eventos` con índices |
| `V11__seed_auditoria_tipos.sql` | Registro semilla de referencia |

### 2.8 Application Properties

- Se agregaron propiedades `app.timezone`, `app.api.version`, `app.jwt.expiration`

### 2.9 Tests Backend

| Archivo | Tipo |
|---|---|
| `MarcaServiceTest.java` | Unit test (Mockito) |
| `RolServiceTest.java` | Unit test (Mockito) |
| `EquipoServiceTest.java` | Unit test (Mockito) |
| `AuditoriaServiceTest.java` | Unit test (Mockito) |
| `MarcaMapperTest.java` | Unit test (Mapper) |
| `RolMapperTest.java` | Unit test (Mapper) |
| `MarcaResourceTest.java` | Integration test (QuarkusTest + REST Assured) |

### 2.10 Tests Frontend Web

| Archivo | Tipo |
|---|---|
| `api.test.js` | Unit test (parseToken) |
| `store.test.jsx` | Component test (AppProvider) |

**Config:** `babel.config.cjs`, `jest` key en `package.json`, setup en `__tests__/setup.js`

### 2.11 Tests Mobile

| Archivo | Tipo |
|---|---|
| `api.test.js` | Unit test (parseToken) |
| `LoadingScreen.test.js` | Component test (render) |
| `EmptyState.test.js` | Component test (render) |

**Config:** `jest` key con `preset: "jest-expo"` en `package.json`

### 2.12 CI/CD — GitHub Actions

| Workflow | Archivo |
|---|---|
| `CI/CD` | `.github/workflows/ci.yml` |

**Jobs:**
- `backend`: Java 17 + PostgreSQL 18 service → `mvn clean verify`
- `frontend`: Node 20 → `npm ci` → lint → test → build
- `mobile`: Node 20 → `npm ci` → lint → test

### 2.13 Frontend Web — Modo Claro/Oscuro

- `ThemeModeContext.jsx` integrado en `main.jsx`
- `main.jsx` ahora usa `createAppTheme(mode)` en lugar de `theme` estático
- El modo se puede cambiar desde cualquier componente con `useThemeMode()`

---

## 3. Módulos Completados en HDT-003

| Módulo | Estado |
|---|---|
| Módulo Rol (Service + DTO + Mapper) | ✅ |
| Paquete audit/ (entidad, repositorio, servicio, controller) | ✅ |
| Paquete config/ (CORS, AppConfig) | ✅ |
| Paquete security/ (JwtFilter, SecurityUtil) | ✅ |
| ManejadorGlobalExcepciones mejorado | ✅ |
| Migración V10 (auditoria_eventos) | ✅ |
| Migración V11 (seed) | ✅ |
| Tests backend (7 archivos) | ✅ |
| Tests frontend web (2 archivos + config) | ✅ |
| Tests mobile (3 archivos + config) | ✅ |
| CI/CD GitHub Actions | ✅ |
| Modo claro/oscuro frontend | ✅ |

---

## 4. Pendientes para Próximo Hito

| Pendiente | Prioridad |
|---|---|
| Rebuild APK con EAS Cloud | Crítica |
| Firebase Crashlytics + google-services.json | Media |
| Módulos PSR/OSR (backend + web + mobile) | Media |
| Evidencias fotográficas (backend + mobile) | Media |
| Dashboard KPI | Baja |
| Reportes PDF | Baja |
| Migrar mappers manuales a MapStruct | Baja |
| Agregar Request DTOs formales | Baja |

---

## 5. Instrucciones de Retorno

Si se retoma el desarrollo desde este hito:

1. Backend, frontend web y mobile completamente funcionales con HDT-001 + HDT-002 + HDT-003
2. Módulo Rol completo con Service + DTO + Mapper
3. Auditoría operativa implementada (tabla V10 + backend)
4. Tests automatizados en backend (JUnit/Mockito), frontend (Jest/RTL), mobile (Jest/RNTL)
5. CI/CD configurado en GitHub Actions
6. Modo claro/oscuro disponible en frontend web
7. Arrancar con siguiente hito: HDT-004 (PSR/OSR, evidencias, dashboard)
8. Prioridad inmediata: Rebuild APK EAS Cloud y Firebase Crashlytics

---

*Documento generado post-implementación. Versión 1.0 — 2026-07-08*

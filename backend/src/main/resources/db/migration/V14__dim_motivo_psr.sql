CREATE TABLE IF NOT EXISTS dim_motivo_psr (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    codigo VARCHAR(50) NOT NULL UNIQUE,
    nombre VARCHAR(200) NOT NULL UNIQUE,
    nombre_corto VARCHAR(100) NOT NULL,
    estado_activo BOOLEAN NOT NULL DEFAULT TRUE,
    fecha_creacion TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
    fecha_actualizacion TIMESTAMP WITH TIME ZONE,
    usuario_creacion BIGINT,
    usuario_actualizacion BIGINT,
    version INT NOT NULL DEFAULT 0
);

CREATE INDEX IF NOT EXISTS idx_dim_motivo_psr_estado ON dim_motivo_psr(estado_activo);

INSERT INTO dim_motivo_psr (codigo, nombre, nombre_corto, usuario_creacion) VALUES
('TRANSPALETA_LITIO', 'SERVICIO DE ALQUILER DE TRANSPALETA ELÉCTRICA - BATERÍA DE LITIO', 'Transpaleta - litio', 1),
('TRANSPALETA_PLOMO', 'SERVICIO DE ALQUILER DE TRANSPALETA ELÉCTRICA - BATERÍA DE PLOMO', 'Transpaleta - plomo', 1),
('APILADOR_LITIO', 'SERVICIO DE ALQUILER DE APILADOR ELÉCTRICO - BATERÍA DE LITIO', 'Apilador - litio', 1),
('APILADOR_PLOMO', 'SERVICIO DE ALQUILER DE APILADOR ELÉCTRICO - BATERÍA DE PLOMO', 'Apilador - plomo', 1),
('MONTACARGAS_LITIO', 'SERVICIO DE ALQUILER DE MONTACARGA ELÉCTRICO - BATERÍA DE LITIO', 'Montacargas - litio', 1),
('MONTACARGAS_PLOMO', 'SERVICIO DE ALQUILER DE MONTACARGA ELÉCTRICO - BATERÍA DE PLOMO', 'Montacargas - plomo', 1)
ON CONFLICT (codigo) DO NOTHING;